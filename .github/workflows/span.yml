name: blank

on:
  push:

jobs:
  readily:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        summit: ["trap", "contrast", "afford", "negotiate", "outstanding"]
        desire: ["consent", "action", "sausage", "undoubtedly"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar mh
